const SidebarFestival = ({ name, time }) => (
  <div className="bg-orange-50 p-3 rounded-lg">
    <p className="font-medium">{name}</p>
    <p className="text-sm text-gray-600">{time}</p>
  </div>
);
export default SidebarFestival;
